---
name: Buttons
---

import { Playground } from "docz";
import { Button, Flex, IconButton, ProgressButton, Badge } from "component-library";

# Buttons

## Basic

<Playground>
  <Flex direction={"column"}>
    <Button variant={"red"}>Button</Button>
    <Button variant={"green"}>Button</Button>
    <Button variant={"blue"}>Button</Button>
    <Button variant={"orange"}>Button</Button>
  </Flex>
</Playground>

## Icon

<Playground>
  <Flex direction={"column"}>
      <Badge>wdqw</Badge>
    <IconButton icon={"TrashIcon"} variant={"red"}>
      Button
    </IconButton>
    <IconButton icon={"TrashIcon"} variant={"green"}>
      Button
    </IconButton>
    <IconButton icon={"TrashIcon"} variant={"blue"}>
      Button
    </IconButton>
    <IconButton icon={"TrashIcon"} variant={"orange"}>
      Button
    </IconButton>
  </Flex>
</Playground>

## Progress

A button that when clicked or pressed on for an amount of time, will display a visual indicator of progression.

<Playground>
  <Flex direction={"column"}>
    <ProgressButton variant={"red"} progressColor={"blue"}>
      Button
    </ProgressButton>
    <ProgressButton variant={"green"} progressColor={"orange"}>
      Button
    </ProgressButton>
    <ProgressButton variant={"blue"} progressColor={"red"}>
      Button
    </ProgressButton>
    <Flex align={"center"}>
      <ProgressButton
        variant={"orange"}
        progressColor={"red"}
        onProgressComplete={() => alert("Do something")}
      >
        Button
      </ProgressButton>
      <small>I have an onComplete handler</small>
    </Flex>
  </Flex>
</Playground>
