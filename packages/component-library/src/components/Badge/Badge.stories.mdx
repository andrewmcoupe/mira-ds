import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { Badge } from "./Badge";

<Meta title="Components/Badge" component={Badge} />

export const Template = (args) => <Badge {...args} />;

# Badge

The Badge component should be used to display some adjacent information about an element.

<Canvas>
  <Story
    name="Primary"
    args={{
      children: "#default",
      variant: "badges.primary",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Success"
    args={{
      children: "#success",
      variant: "badges.secondary",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning"
    args={{
      children: "#warning",
      variant: "badges.ghost",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
