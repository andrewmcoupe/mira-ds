import { Canvas, Meta, Story, Preview, Props } from "@storybook/addon-docs";

import { Badge } from "./Badge";

<Meta title="Components/Badge" component={Badge} />

# Badge

The Badge component should be used in situations when a user can perform an action.

### Usage

```jsx
import { Badge } from "@mira-ds/components";

<Badge>Learn more</Badge>;
```

### Primary variant

<Preview>
  <Story
    name="Primary"
    args={{
      variant: "badges.primary",
    }}
  >
    {(args) => <Badge {...args}>react</Badge>}
  </Story>
</Preview>

### Secondary variant

<Preview>
  <Story
    name="Secondary"
    args={{
      variant: "badges.secondary",
    }}
  >
    {(args) => <Badge {...args}>javascript</Badge>}
  </Story>
</Preview>

### Tertiary variant

<Preview>
  <Story
    name="Tertiary"
    args={{
      variant: "badges.tertiary",
    }}
  >
    {(args) => <Badge {...args}>webdev</Badge>}
  </Story>
</Preview>
