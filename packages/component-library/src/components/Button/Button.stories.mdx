import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { Button } from "./Button";

<Meta title="Components/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

The Button component should be used in situations when a user wants to perform an action.

<Canvas>
  <Story
    name="Primary"
    args={{
      children: "Button",
      variant: "buttons.primary",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary"
    args={{
      children: "Button",
      variant: "buttons.secondary",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Ghost"
    args={{
      children: "Button",
      variant: "buttons.ghost",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
