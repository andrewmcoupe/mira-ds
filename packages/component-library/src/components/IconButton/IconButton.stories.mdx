import { Canvas, Meta, Story, Preview, Props } from "@storybook/addon-docs";

import { IconButton } from "./IconButton";

<Meta title="Components/IconButton" component={IconButton} />

export const Template = (args) => <IconButton {...args} />;

# Button

The Icon Button component should be used in situations when a user can perform an action.

### Usage

```jsx
import { IconButton } from "@mira-ds/components";

<IconButton icon={"SunIcon"} />;
```

### Primary variant

<Preview>
  <Story
    name="Primary"
    args={{
      variant: "buttons.primary",
      icon: "SunIcon",
    }}
  >
    {(args) => <IconButton {...args} />}
  </Story>
</Preview>

### Secondary variant

<Preview>
  <Story
    name="Secondary"
    args={{
      variant: "buttons.secondary",
      icon: "SunIcon",
    }}
  >
    {(args) => <IconButton {...args} />}
  </Story>
</Preview>

### Tertiary variant

<Preview>
  <Story
    name="Tertiary"
    args={{
      variant: "buttons.tertiary",
      icon: "SunIcon",
    }}
  >
    {(args) => <IconButton {...args} />}
  </Story>
</Preview>
